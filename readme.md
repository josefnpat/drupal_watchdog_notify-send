This script watches for new entires on the watchdog table and pipes it out to notify-send.

Configure the config.php, and then run the script (`nohup ./drupal_watchdog_notify-send.php &` if you want it in the background)
